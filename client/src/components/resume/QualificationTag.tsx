import React from 'react';
import { AlertCircle, CheckCircle, XCircle } from 'lucide-react';

interface QualificationTagProps {
  isOpen: boolean;
  onClose: () => void;
}

export default function QualificationTag({ }: {}) {

}
