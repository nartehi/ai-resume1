import React, { useState, useRef, useEffect } from 'react';
import { User, Settings, LogOut, Crown, Shield, ChevronDown } from 'lucide-react';

interface UserMenuProps {
  onOpenSettings: () => void;
}

export default function UserMenu({  }: {}) {
}
