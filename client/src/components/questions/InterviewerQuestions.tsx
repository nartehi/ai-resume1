import React, { useState, useEffect } from 'react';
import { MessageCircle, X, Code, Users, Loader2, RefreshCw } from 'lucide-react';
import { JobData } from '../../types/index';
// import { generateInterviewerQuestions } from '../services/api';

export default function InterviewQuestions({  }: {}) {
}